# 更新日志

格式参照 Keep a Changelog；版本号遵循语义化。链接各迭代提案。

## 未发布

### it-040 — 已录入衣物补抠（W5 状态条）
- W5 大图下新增去背景状态条：原图态「去背景」/ 已抠态「重新抠图」（alpha 采样检测，无状态字段），推理中进度态，候选预览期棋盘格 + 还原/保留。
- 补抠走「候选新图」模式（ADR-023）：推理产独立新文件、当前图全程不动，保留才换图删旧、还原即弃候选；跨会话不可撤销如实呈现。
- 阶段 A spike 放行：WebP q82 往返透明区 RGB 零清黑、alpha 代际 IoU=1.0、u2netp 二次分割 IoU ≥0.945（[报告](../../reports/2026-09-25-cutout-recut-spike/)）。
- 详情见 [it-040](iterations/it-040-backfill-cutout.md)；specs/01（US-40 + US-15 AC 修订）/02/06（ADR-023）同步更新。

### it-041 — AI 模型接入（阶段 A 设置页 + 阶段 B 对话页）
- **阶段 B**：W12 对话页（穿搭顾问）——流式打字机 +「已查衣橱」工具条 + 分类错误重试 + 停止即停；4 个只读工具（search_items / search_outfits / wear_stats / current_person）真查衣橱再回答；FileSessionStore 杀进程续聊（调用方契约：用户消息发送即入会话）；W11 用量卡（厂商×模型 count-up）；演示模式挂离线 FakeChatModel（工具仍真查演示数据，零外呼）。
- 阶段 B 修复：Wire 层 `tools[].type` / `tool_calls[].type` 在 encodeDefaults=false 下被省略（GLM 1214 实测拒绝）→ `@EncodeDefault` 强制编码 + 契约回归测试。
- **阶段 A**：新增 W11 设置页（W3 标题行 ⚙ 入口）：厂商（智谱 GLM / MiMo 按量 / Token 套餐 / 自定义）与模型档选择、API Key Keystore AES-GCM 密文存取（界面只显 mask）、「保存并自检」1-token ping 按错误分类呈现，自检成功给确认触感；清除 Key 二次确认。
- 接入 libs/agent SDK（includeBuild + `libs.leo.agent`），Manifest 新增 INTERNET 权限（唯一用途 BYOK 模型直连，ADR-024）；演示模式禁用 Key 输入并永不挂真 Key。
- 设置页内「退出演示模式」获得真实落点（原 WardrobePackages 提示文案指向不存在页面的问题随之消除）；关于卡显示数据模式、版本与联网边界注记。
- SDK 侧修复：`OkHttpChatModel.complete` 切 IO 调度器（NetworkOnMainThreadException，M1 遗留）。
- 详情见 [it-041](iterations/it-041-ai-integration.md)；specs/00/01/02/04/06（ADR-024）同步更新。

### it-037～039 — 衣橱 UI 走查与交互优化
- W3 浮动新增按钮改为不遮挡列表的固定全宽 CTA；底部 Tab 与 W2 当前角色精简选中线索。
- 浅色强调色拆分图形色与文字/动作色，提升弱色文字对比；W1 槽位长名称单行省略并保留完整无障碍名称。
- W10 表单统一文案与描边，W4/W10/W5/W7 标签录入行增加按滚动状态显示的渐隐提示；评论删除与发送图标更明确。
- 详情见 [it-037](iterations/it-037-ui-redlines.md)、[it-038](iterations/it-038-contrast-tokens.md)、[it-039](iterations/it-039-ui-layout-polish.md)；specs/01/02/05 同步更新。

### it-036 — 组件一致性收尾：chips 渐隐 / 分段共享 / 导出弹层 / 心愿占位 / 文案格式
- 新增 `ScrollFade`（W3/W8/W10 筛选行右缘 28dp 自适应渐隐）与 `SegmentedToggle`（从 W9 抽出，W9/W10 分段共用同款，伪分段废止）。
- W6 导出弹层文案区可滚至完整 + 底缘渐隐、长图标签 ellipsis+8dp、两输入框占位符式统一；W10 八品类色块占位 + 域名弱化独立行；「随机一套」与评论日期 YYYY/MM/DD 全站统一；W1 混入心愿 accent 会话提示（一次/5s 隐）。
- 详情见 [it-036](iterations/it-036-component-cohesion.md)；spec 02/05 同步注记。

### it-035 — 素材管线校验（走查根因修复）
- 新增 `tools/mock-data/validate_assets.py`：尺寸比例 / 背景纯净度 / 底缘残片三层判定 / 效果图边距提示，`--strict` 可挂 CI。
- 基线：1 FAIL（两批规格 362²×24 + 410×319×12 混用）+ 8 WARN（6 张残片视觉实锤 item-03/04/06/16/19/20，成因=裁剪跨进源图邻物）；素材二进制零改动，重出净化待 it-032 合入后单独执行。
- 详情见 [it-035](iterations/it-035-asset-pipeline-check.md)。

### it-034 — 页面骨架与状态语义（走查 C4/C5/C6/C9）
- 四白底路由（W4/W5/W7/W9）关根 Scaffold 顶部 inset → 状态栏沉浸同色（像素实测纯白，Tab 页原状保留）；W7/W10 删除补 error 红——二次确认核实 it-029 已存在，评论项系走查误报。
- W9：品类分布 6 档绿色阶+宽段段内直标；年度长图按 `hasWearData` 补解锁说明与达标对比度；数据包行 demo 态降透明+行内徽标；衣柜提醒开关联动、选中态统一品牌绿。
- 详情见 [it-034](iterations/it-034-skeleton-state-semantics.md)；spec 02 同步注记。

### it-033 — 触控与截断专项（走查 C1/C3/C7 高频 P1）
- W1 名称条三段式不截断（溢出动态缩字号 11→8sp → maxLines=2 → 省略兜底），一屏网格不动；n/n 角标补 a11y「第 n 件，共 m 件」；✕ 视觉 16dp、热区 28×44dp。
- 触控基线全站落地：📊🌟⋮ / 品类 chips / 标签 chips / 分页胶囊 / 评论 × 显式 ≥44dp（推荐 48dp），spec 05 立「触控目标基线」；W3 卡标题两行恒行高、W8 分页拆 ‹› 双半热区。
- 走查「18dp」口径勘定为字形子节点 bounds（真实原热区 32dp）已记入 spec；详情见 [it-033](iterations/it-033-touch-targets-truncation.md)。

### it-032 — 演示数据集扩充与图片随 APK 编译
- DEBUG 演示模式改从 `assets/mock/wardrobe.json` 读取完整数据：2 角色、36 单品、9 套穿搭、心愿域与评论；新增 36 张单品图 + 3 张效果图，随 APK 离线打包。
- `AppContainer` 统一解包内置 mock 图片，`MockWardrobeData` 增加 JSON 解析入口；真实衣橱数据与图片目录不受影响。

### it-031 — 版式收拢：名称条 / 翻页器 / 吸底动作栏 / 打卡层级 / 空态行动（审查 C5–C7/C9/C10）
- W1 名称条「名称优先」：品类前缀废止 → 纯 n/n 序号角标（可点循环翻页，P1 穿透修复）+ ✕ 换 Material Close；格间距 12dp、小格系数 0.85，杜绝截断且一屏未破；
- W8 翻页胶囊外置卡下方居中（帽行不再被压），两侧圆钮废止（it-015 修订二覆盖）；
- W7「今天穿了这套」= 唯一实底主按钮，「复制长图」降描边；W10 种草表单固定动作栏（收进想买吸底常驻）；W9 打卡空态卡增「去打卡」直达记录 Tab；
- 详情见 [it-031](iterations/it-031-layout-pass.md)；spec 02 增 it-029~031 交互注记。

### it-030 — 去 emoji 化与字体纪律（审查 C3/C4）
- 功能图标 emoji → Material Icons（心愿 Star、复制 ContentCopy、拍照 PhotoCamera、随机 Casino 等，含补遗：Wishlist📋 按钮/🔗 前缀、💬 评论标题×2、📦/⚠ 提示前缀）；文本提示语 emoji 清零；
- 衣物卡/心愿卡实体名统一衬线 Title（单行省略）；品类 emoji chips（it-012）留待专项裁决；
- 详情见 [it-030](iterations/it-030-deemoji-and-serif.md)。

### it-029 — 止血：P0 崩溃修复 + 评论删除确认 + 导航/弹层色阶（审查 C1/C8/C2）
- 修 P0：混入心愿关闭时格位 `items[page]` 越界崩溃（页码回卷防御，it-019 引入）；
- 评论删除出确认对话框（DESIGN.md §5.7 红线回正，W5/W7 共用）；
- `surfaceContainer*` 五档覆盖：底部导航/弹层紫灰默认值 → 纸感家族；
- 详情见 [it-029](iterations/it-029-stop-bleed.md)。

### it-028 — 质感 pass R2：触控目标 + 首屏入场落地 + 触感补线（DESIGN.md 对表第二遍）
- 触控目标：评论删除钮 28→44dp、卡片「···」媒体角标 36dp（新例外档入 DESIGN.md §2.5）；
- 列表入场落地：新增 `StaggeredEntrance`，衣橱/穿搭记录两网格 24ms 错峰 fade+上移，`rememberSaveable` 仅首进播放（此前 spec 05 #7 声明存在但代码缺失，spec-code 不一致就此结清）；
- 导出面板「已复制 ✓」2s 复位（与存相册同拍）；
- 触感补线 ×5：角色编辑/新建保存、心愿「收进想买/保存」、心愿购入转正、心愿穿搭升级 = confirm。

### it-027 — 质感 pass：触感基线 + 统计数字动效（DESIGN.md 首轮落地）
- 触感反馈从零到一：新增 `ui/components/Haptics.kt`（confirm/error/tick，API 30+ CONFIRM/REJECT、低版本回退），接线复制长图 ✓、存相册 ✓、☆保存这套、🌟存为心愿、穿搭打卡、去背景成功（confirm）；评论发送、撤销今日打卡、还原原图（tick）；未就绪点保存（error）；
- W9 三大数字 count-up（`CountUpText`：首进 0→N、今年/累计档位切换旧值过渡，smooth 弹簧 60ms 错峰）；
- DESIGN.md 红线对表：对比度全部达标（inkFaint/paper 3.26:1）、深色按钮纸字 ≈9:1，无需改值。仓库级基线同步入库：根目录 [DESIGN.md](../../../DESIGN.md) v1.0，AGENTS.md 上下文恢复已引用。

### it-026 — 数据包交互加固（走查 P0/P1 修复）
- 替换危险态三件套：「当前：替换模式」警示胶囊常驻 + 替换单选整行警示红 + 确认按钮随模式变「替换…」着警示色；
- 替换明细删除/导入两侧对称补图片数（含二次确认文案）；二次确认「先导出一份当前数据（推荐）」升为可执行按钮、确认措辞改「仍要替换」；
- 差异行 ＋/↻/＝ 与错误弹窗 ✗/✓ 符号分层着色（绿/琥珀/灰/红）；导入入口副标题补「（覆盖前确认）」风险暗示；
- 追加轮（P2 打磨）：数据小节行图标加 40dp 圆底、合并预览零变化集合折叠为「其余 N 项无变化」汇总行、错误多原因行距分组。
- 依据：[数据包功能 UI 走查报告](../../../reports/2026-09-21-data-package-audit/) P0×2/P1×3/P2（编号自 it-025 顺延——原号被 UX 评审落地占用）。

### it-025 — 衣橱卡交互语义修正 + 回顾空态引导（UX 评审落地）
- 衣橱卡片单击从「进编辑」改为进 W5 详情（大图/评论/穿搭反查直达；名称行尾 › 暗示），编辑唯一入口收到右上 ··· 菜单（长按删除不变）；W9 回顾无打卡数据时不再渲染「穿 0 次/利用率 0%」空指标，改为打卡引导卡。（UX 报告 W1·P1 + P2，编号自 it-024 顺延——原号被数据包提案占用）

### it-024 — 结构化数据包：导出 / 导入 / agent 加工回流
- W9 新增「数据」小节：导出 `wardrobe-backup-*.zip`（manifest + wardrobe.json + images），完成带 [分享]；导入支持合并（默认，按 id 实体级覆盖、本地多出保留）/ 替换（二次确认），预检失败零改动、导入即时生效；
- 系统直达导入：微信「用其他应用打开」/ 文件管理器「分享 → 衣橱」免选文件直达校验+预览流程；
- libs/store 0.2.0 新增 PackageCodec（zip 编解码 + manifest 契约 + SnapshotStore.upgrade），ADR-022；
- 仓库级 agent skill `.agents/skills/data-package/`：两 app schema 分册 + validate.py 校验器（与 app 预检同规则）+ 示例包。

### it-023 — UI 三轮走查与交互修复（夜间专项）
- 修复 ModalBottomSheet 内校验提示被遮挡（转正无实物照/心愿名为空时提交按钮置灰并明示原因）；
- 长图预览改 Coil 异步降采样（原组合期主线程全尺寸解码 ~14MB，开预览必卡）；
- 三轮模拟器走查（uiautomator+截图+交互流实测）全程零崩溃；排除 3 项疑点（详见 it-023）。

### it-022 — 性能梳理与优化（夜间专项）
- 衣橱/记录页过滤链包 remember、4 处 Lazy 行补 key；审计确认落盘/长图渲染/图片导入均已在 IO 线程（详见 it-022）。

### it-021 — 结构拆分（阶段 1）
- Repository 按聚合域拆 6 子接口（门面继承，调用点零改动）；ImageEditStore 接口收编；RecapViewModel 拆出；「好久没穿」候选规则抽 domain 纯函数 StaleItemSelector（+4 单测）；WishlistViewModel 拆分经评估裁剪（理由入档）。

### it-020 — 架构评审止血：Mock 级联对齐 + 写路径兜底 + 常青 spec 对齐
- MockWardrobeRepository 补齐 deletePerson / deleteItem 心愿域级联（此前与真实实现漂移，演示模式数据不一致；+2 单测锁定语义一致）；AppViewModel 写路径统一异常兜底 `launchSafely`（此前 33 个 repo 调用点仅 5 条新写路径有捕获，其余 commit 失败会崩进程）；00/04/06 spec 与代码对齐（范围外清单勘误、模块树、路由表、单 AppViewModel 现状、ADR-016/017 排序）；随附仓库级护栏：CI 单测矩阵（六构建）+ 根 version catalog。来源：[架构评审报告 2026-09-20](../../reports/2026-09-20-architecture-review/report.md)。

### it-019 — 心愿清单：想买单品 + 心愿穿搭（混搭预览与购入转正）
- 新增 WishItem 想买单品与 WishOutfit 心愿穿搭双实体（W10 心愿页两分段），W1「🌟 混入心愿」把愿望单品混进搭配槽位上身预览、组合「🌟 存为心愿」可反复回看（Leo 拍板可保存），「已买到」一键转正回填购入记录、心愿穿搭自动更新，买齐后「👗 升级为穿搭」移交预览图为成品图；含愿望件组合禁存 Outfit（仅预览），旧 JSON 向后兼容零迁移。（ADR-020）

### it-018 — 穿搭打卡 + 衣橱回顾 + 好久没穿提醒
- 新增 WearLog 穿搭打卡（W7 详情一键「今天穿了这套」，同日多套/撤销今日，级联删除与悬空清洗，
  wearLogs[] 向后兼容不 bump schemaVersion，US-17 / ADR-018）；
- 新增 W9 衣橱回顾页（三大数字、最百搭 TOP3、品类分布、利用率、闲置清单二级页、出勤最高、
  连续打卡；今年/累计两档，入口=衣橱页标题行 📊，US-18）；
- 「年度衣橱」长图（按角色出图：封面/三大数字/最百搭/品类/成品图墙 ≤9/结尾，存 Pictures/Wardrobe +
  分享复用导出门面，US-19）；
- WorkManager「好久没穿」本地提醒（单品粒度：穿过 ≥2 次·超 N 天，文案带角色名，每日至多 1 条，
  默认关，点击深链进详情，ADR-019，US-20）；RecapCalculator/Repository 级联单测。

### it-016 — 录入去背景（libs/cutout 阶段 A + wardrobe 阶段 B）
- 新增 libs/cutout 主体抠图 SDK（u2netp+ONNX 纯 JVM，15 单测含真模型自测，报告 reports/2026-09-20-cutout-sdk-test）；wardrobe W4 新增「去背景 · 一键透明底」（仅新照片）：离线推理、棋盘格透明预览、一键还原、保存采用后原图即弃；模型打包 assets 全离线；release 52.8MB（+33.6MB）；ADR-016 / US-15

### it-017 — 角色形象参考照（可选）
- Person 新增可选 refImageFile（复用压缩管线，删角色/换照级联清理，旧 JSON 兼容）；
  编辑角色对话框设置（选图/更换/移除），导出时长图顶部拼入人像+标注条、文案追加形象还原句；
  ExportSheet 新增「附形象参考照」开关（默认开、不持久化，关闭仅本次），W7 复制素材通道同步携带（ADR-017）。
- 修订（Leo 走查反馈）：长图改**人体比例拼贴**（与 W1/BodyCollage 同构：帽/上身行/腿行挂件/鞋行，
  典型 1024×~3500，弃 ~9900 全宽竖堆）；**Prompt 移至长图顶部**（EXIF/XMP meta 保持）；
  文案措辞随布局更新。

### it-015 — 应用内演示模式（Mock 数据源）
- data/mock/：确定性种子（两角色/17 件/五组合/三笔记，assets 内置 thiings 照片 2.2MB）+ 内存 Mock 仓库（不落盘）；
  DemoMode 开关组合根切换、重启进程生效，入口仅 DEBUG 可见、进/出同一入口确认（ADR-015；修订：去横幅）。

## [0.5.0] — 2026-09-21 首个公开 release（it-011~it-019：演示模式/抠图/形象参考照/穿搭打卡+回顾/心愿清单，版本对齐见 ADR-021）

## [0.4.6-it010] — 2026-09-20

### it-010 — 卡组卡面结构化与层级修正
- 卡面：成品图优先全幅；否则按真人比例迷你结构拼贴（帽/上身行/腿+挂件/鞋），弃 2×2 罗列
- 层级：卡组容器裁剪（滑出卡不越界遮挡）、堆叠偏移 30→14dp、投影 4→2dp；CardDeck SDK 增加 properties 透传

## [0.4.5-it009] — 2026-09-20

### it-009 — 衣橱列表品类 Tab 筛选
- 顶部品类 Tab 行（横滑，3D 图标 + 单选）：全部/8 品类，直达品类免长滚；选中时标题与计数随动，与标签筛选叠加

## [0.4.4-it008] — 2026-09-20

### it-008 — 搭配页真人比例重排
- 上身行全宽三等分（外套/上装/连衣裙，不再被挂件挤占）；下装改窄长腿型（通栏→窄卡）
- 包/配饰挂腿两侧留白；鞋 0.82→0.58 宽小扁；头-上身-腿-脚真人比例轮廓

## [0.4.3-it007] — 2026-09-20

### it-007 — 穿搭记录卡组化
- W8 顶部侧滑卡组（libs/carddeck，与 eats it-003 共用）：穿搭大卡快速浏览 + 「随机一套」纯随机抽取；下方保留全量网格
- 演示数据补 5 套已保存穿搭；接线 includeBuild(carddeck) + JitPack 仓库

## [0.4.2] — 2026-09-20

### R6 主题重塑（应用户反馈，与 eats 同批）
- 砖红强调色整体切换为清新绿系（青杉绿 #429E68 / 暗色 #74C790），纸底改淡绿白，暗色改墨绿纸
- 彩屑四色、启动图标底色、M3 容器色同步；排版与动效不变

## [0.4.1-it006] — 2026-09-20

### it-006 — 交互与素材统一性微升级（与 eats it-002 同批评审驱动）
- 搭配页主操作 Button → FilledTonalButton（评审：全宽实心压迫感过强）
- 衣橱列表行距 4dp → 8dp（评审：呼吸感）
- 新增 `tools/demo-data.sh`：17 件衣物演示数据（thiings 服饰素材作品类照片），供评审/联调
- R5 亮/暗双模式截图评审：搭配页 8.5/10、衣橱列表 8/10，无阻塞问题

## [0.4.0-it005] — 2026-09-20

### it-005 — 搭配页人体着装位布局（换装游戏式）
- 卡片位置=穿着位置：帽（头）/外套·上装·连衣裙（上身行）/下装（腿通栏）/鞋（脚扁平）/包·配饰（两侧挂件）
- 单品名改为卡内底部遮罩条；各部位卡片比例区分；移除第 9 添加格（空品类格即 ＋ 入口）
- 滑动换衣/组合记忆/保存去重/复制长图全部沿用；QA 一屏与去重状态实测通过

## [0.3.0-it004] — 2026-09-20

### it-004 — 搭配页直接保存穿搭 + 组合去重检测
- W1 底部新增 ☆保存这套：未保存创建、已保存（单品集合相同即命中，顺序无关）变 ★ 并可点击直达穿搭详情
- 去重判定基于组合记忆推导（与 pager 时机解耦）；导出面板收藏同走去重
- 域层新增 `outfitWithItems` 集合相等查询 + 4 个单测；QA 实测保存/★/重启恢复/无重复全通过

## [0.2.1] — 2026-09-20

### fix(wardrobe): 添加衣物保存必现失败（用户真机反馈）
- 根因：`BitmapFactory` 在 `inJustDecodeBounds` 尺寸探测模式下按设计返回 null，原代码误将其当失败信号，导致任何照片导入必然失败（it-001 起存在，模拟器验证未覆盖该链路）
- 修复：以 `bounds.outWidth/Height` 判定尺寸解析结果；照片改为选中瞬间即导入落盘（消除授权过期窗口）；导入/保存失败分支补全日志与细分提示、协程异常兜底
- QA 模拟器全流程实测通过：选图即时导入 → 填名保存 → 列表/搭配页联动

## [0.2.2-it002-sdk] — 2026-09-20

### 二轮 review：SDK 过度设计精简
- 删除零消费方/投机性 API（store：BackupCodec/sweep/list/LoadOutcome/decode；sync：增量游标/冲突策略接口/进度计数/队列接口/附件元信息等），均记录于两库 specs「实现状态」节，按需再加
- 测试 88 → 82 全绿；wardrobe 集成复验通过

## [0.2.1-it002-sdk] — 2026-09-20

### it-002（云同步前置）— 两套公共 SDK 落地 + wardrobe 数据层接入 store SDK
- `libs/store` 0.1.0：SnapshotStore（原子写/三级恢复/迁移链）、SsotRepository（writeHook 同步缝）、FileMediaStore、BackupCodec（zip）——20 单测
- wardrobe 接入：JsonFileStore 退役；WardrobeRepositoryImpl 继承 SsotRepository；ImageFileStore 文件管理改走 FileMediaStore；行为与备份格式不变，单测全绿、assembleDebug 通过
- `libs/sync` 0.1.0：契约（SyncValue 七值 / SyncEngine 轻同步 / FilePendingOpQueue / SyncError 折叠）+ feishu-bitable 适配器（自动建表补列、UI 手建行收编、WriteGate 串行 + 429 退避）——49 单测；**按计划暂不接入 App**
- 架构 review：依赖边界 grep 审计通过；修复引擎 3 处缺陷并补状态机测试；ADR-011/012、AGENTS.md、各 specs 同步更新

## [0.2.0-it002] — 2026-09-20

### it-002 — 导出重构：单张长图 + 多维度 Prompt 选择器
- 「复制长图」：按品类槽位顺序从上到下拼纵向长图（1024 宽），Prompt 绘制于图片底部，并写入 XMP 元数据（UTF-8；EXIF 因 androidx 库中文必损而弃用，ADR-007）
- Prompt 五维度预设选择器：场景/氛围/季节/光线/构图（单选可清除），实时组装可编辑文案
- 人物描述输入（DataStore 持久化记住）
- 保留「只复制文本（含单品清单）」「分享」辅助通道；收藏标签 = 已选维度值
- 单测 18/18；模拟器实测长图/XMP/联动全通过（iterations/it-002-export-long-image.md）

## [0.1.0-it001] — 2026-09-19

### it-001 — MVP（模拟器验证通过，待真机验收）
- 多角色衣橱：角色切换/管理、8 品类、级联删除
- 衣物录入（Photo Picker + EXIF 摆正 + WebP 压缩）、编辑、滑删确认、标签筛选
- 搭配页：品类槽位轮播（缩放/透明形变）、🎲 老虎机随机、组合记忆（修复 DataStore 竞态）
- 导出：2 列网格合成图 + 自动可编辑文案（含风格标签），复制图片+文本（FileProvider URI）/只复制文本/分享兜底
- 穿搭体系：收藏、穿搭记录（成品图优先/拼贴占位）、详情、成品图录入、单品↔穿搭双向关联
- 标注体系：单品/穿搭打标（预设+自定义）+ 自然语言评论时间线
- 15 个 JVM 单测；模拟器逐页实测（见 iterations/it-001-mvp.md 验证矩阵）

## it-011 · UX 评审落地（可发现性与叙事）2026-09-20

- 报告：`reports/2026-09-20-ux-review/` → 本迭代吃掉 R1–R8
- O6 格位 ‹n/n›+首次 coach+按钮主次；O7 BodyCollage 人体拼贴组件（W8 卡面/W7 主视觉共用+缺失空槽+单品行可点）
- O8 导出面板折叠四维+记住选择+首屏即达复制；O9 角色当前态「✓ 使用中」
- O3 添加衣物吸底保存两态+imePadding；C6 衣橱两列网格+筛选收纳+长按删除（ADR-014）
- C5 PhotoCard 衬纸模式全局统一照片观感；libs/carddeck 增 currentIndex

## it-012 · R2 评审落地（可发现性补课与导出收口）2026-09-20

- 报告：`reports/2026-09-20-ux-review-r2/` → O4' P0 长按删除改 ··· 菜单+筛选固定+图标 Tab+去小节标题/色点胶囊；O5' W8 网格缩略统一人形拼贴；O6' 导出三修：动作栏钉住（滚动区+固定栏结构）/预览 42%+Fit 全貌（顺带修内层滚动约束致 Coil 空白的历史 bug）/记忆竞态 bug（exportSelectionsReady）；O7' 角标移右上+「复制长图」命名统一+照片大占位+保存栏同构重构；R1 名称条合并、R2 使用中紧跟、轮廓对比与衬纸 padding 微调

## it-013 · 导出：自定义 Prompt + 全维度可选 2026-09-20

- Leo 需求：导出支持自由写一段 prompt；标签/场景/更多维度全部可选
- 新增「自定义要求」输入（可选，追加文案末尾「另外要求：…」，PrefsStore custom_prompt 记住上次，长图/文本通道带出）
- 五维（场景/氛围/季节/光线/构图）统一收进「风格与场景维度（全部可选）」折叠区，默认收起；零选择时文案正常生成、收藏允许零标签

## it-014 · 导出：长图保存到相册 2026-09-20

- Leo 需求：图片支持下载而不仅是复制
- 动作栏加「存相册」：MediaStore 两段式写 Pictures/Wardrobe（API 29+ 免权限；旧系统 toast 引导走分享）
- 成功反馈落按钮（「已存相册 ✓」2s）——snackbar 在 sheet 打开时被遮挡不可见
- 启动图标换「木衣柜」（thiings 素材，署名不变）：原空衣架 → 衣柜柜体挂衣更贴App主题

### it-015 修订 — 演示入口改隐藏连点（2026-09-21）
- 演示模式入口从 DEBUG 构建可见的 Science 小圆钮改为全构建可用的隐藏开关：衣橱页标题连点 5 次（3 秒窗口）触发进入/退出确认，第 2 次起 toast 递进提示；正式包亦可体验演示数据

### it-015 修订二 — 搭配格可增删（2026-09-21）
- 搭配页每分区只渲染已加入的品类格，区尾「＋」弹层添加品类、格底 ✕ 移除；上身件数随季节自由（夏天 1 件短袖/冬天内搭+外套），不再固定三格；随机/保存/导出只含已加入件；演示种子修正季节冲突（o1 短袖+羽绒服 → 长袖衬衫+羽绒服）

### it-015 修订三 — 穿搭记录卡组双向循环（2026-09-21）
- 卡组滑到末尾不再清空（循环回首张，libs/carddeck 修订），支持手势往回翻与 ‹ › 按钮双向循环；堆叠层数用库默认（State 首参实为初始索引而非堆叠数，透传会越界空白，已移除该参数）
