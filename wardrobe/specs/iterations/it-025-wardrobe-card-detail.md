# it-025 · 衣橱卡交互语义修正 + 回顾空态引导（UX 评审报告落地，wardrobe 侧）

- **状态**：实施中（2026-09-21，ux-report-impl worktree；编号让位说明：UX 报告原拆分建议写 it-024，该号已被「数据包」提案占用，顺延为本号）
- **来源**：[UX 评审报告 2026-09-21](../../reports/2026-09-21-ux-review/) W1·P1 + P2 + C1/C2

## 范围（对照报告落地拆分）

1. **O1（P1）卡片单击 → W5 详情**：ItemCard `combinedClickable(onClick)` 从 `onEdit` 改为 `onOpenDetail`（WardrobeScreen 新增 `onOpenItem` 接线，MainActivity 复用既有 `openItem`）；
2. **O2（P1）菜单语义收窄**：右上 ··· 菜单保持「编辑/删除」（编辑入口唯一化到菜单，长按删除快捷保留）；更新 W3 spec 描述；
3. **O3（P2）› 提示**：卡片名称行尾加「›」暗示可点进详情；
4. **O3b（P2）W9 空指标 → 空态引导**：`!stats.hasWearData` 时不渲染「最百搭 TOP3（穿 0 次）」与「利用率 0%」块，替换为打卡引导卡（去穿搭详情点「今天穿了这套」）；品类分布（结构指标）保留。

## 不做

- W6 导出预览放大：报告已标「it-017 已知取舍，留待后续迭代」；
- W7 详情「白T恤」显示绿 T：Leo 真实数据命名问题，非代码。

## 验证记录

- 2026-09-21：wardrobe 56/56 单测绿；模拟器实测——卡片单击直达 W5 详情（白T恤详情大图/标签/穿搭反查，w1 截图）；··· 菜单「编辑/删除」仍在、菜单编辑仍进表单；W9 无打卡数据时显示「还没有穿搭打卡」引导卡、0% 利用率块不再渲染（w2 截图）。全程 0 crash。
