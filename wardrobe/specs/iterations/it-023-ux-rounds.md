# it-023 · UI 走查与交互修复（三轮，2026-09-21 夜间专项）

- **状态**：已完成（2026-09-21，arch-review-fixes worktree）
- **来源**：Leo 夜间验收指令——架构/性能/UI 三轮迭代，明早验收「交互良好、bug free、性能良好、功能不丢」
- **走查方式**：模拟器（emulator-5554）实装 debug 包，uiautomator 断言 + 全页截图 + 核心交互流逐条实测

## 走查覆盖（三轮累计）

**wardrobe**：搭配(W1 随机一套/槽位翻选/保存/复制长图) · 穿搭记录(卡组/详情) · 衣橱(网格/筛选) · 单品详情(打卡) · 导出面板(三通道/文案实时生成) · 回顾(W9 指标/长图) · 心愿(W10 两分段/详情/转正表单/心愿穿搭升级) · 演示模式(退出→重启→真实数据→往返) · 角色切换 · 深链。
**eats**：干啥(W1 分类 chips/卡组/随机抽/落定条) · 记一笔(W6 评分/落账/拔草撤销 snackbar/撤销闭环) · 地图(W2 三色 marker) · 列表(W3 搜索/筛选) · 详情(W5 统计/链接/去过记录) · 回顾(W7 年度长图生成/存相册)。

## 发现与修复

| 级别 | 问题 | 修复 |
|---|---|---|
| P2 | ModalBottomSheet 内校验 toast 被 sheet 遮挡（转正无照片/心愿名为空时提示不可见，表单像「点了没反应」） | 提交按钮置灰 + 按钮文案明示原因（「先拍一张实物照」）；WishEditSheet 名空置灰（7cf71ec） |
| P2 | 回顾长图预览在组合期主线程 `BitmapFactory.decodeFile` 全尺寸解码（~14MB），开预览必卡 | 改 Coil AsyncImage 按约束降采样（a684e0b） |
| 排除 | 回顾页顶栏比心愿页高 | TopAppBar 含分段按钮自然增高，非 bug |
| 排除 | 衣橱顶栏图标无障碍 | 已有 content-desc/emoji 文本 |
| 排除 | eats 种草入口「找不到」 | 设计如此：种草在编辑表单开关，详情页仅愿望条目显示取消种草 |
| 观察 | eats 地图状态栏区截图像素噪点 | 仅 screencap 伪影，实机走查两轮 UX 评审未 Flag，不修 |

## 验证记录

- 2026-09-21 三轮走查全程 **0 crash**（logcat crash 缓冲全程监控）；两 app 核心交互流（随机/保存/打卡/落账撤销/转正/升级/长图生成存相册/演示往返）全部实测通过。
- 修复后复验：置灰按钮实测生效（演示模式 + 真实模式各一轮）。
