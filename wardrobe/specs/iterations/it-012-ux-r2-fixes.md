# it-012 · UX 评审 R2 落地（可发现性补课与导出收口）

- **状态**：已完成
- **提案日期**：2026-09-20
- **来源**：`reports/2026-09-20-ux-review-r2/`（用户确认「完整实施优化」）
- **范围**：R2 报告 wardrobe 侧 P0×1 + 全部 P1 + 可落地 P2（O4'–O7'）

## 变更清单

### O4' · 衣橱列表可发现性（R5，P0 + P1×2）

- **··· 显式菜单（P0 修法）**：网格卡右上「···」→ DropdownMenu（编辑/删除，删除仍走确认弹窗）；
  长按保留为快捷路径。
- **「筛选」常驻可达（P1）**：布局改为 `Row { 图标Tab行(weight, 横滑) ; 筛选按钮(固定) }`——
  筛选不再被品类 Tab 挤出屏，角标恢复有效。
- **品类 Tab 图标化（P1 减轻）**：品类 chip 去文字改 16dp 3D 图标圆 chip（「全部」保留文字），
  行长缩短；超出部分横滑。
- **去小节标题（P1）**：「全部」下按品类序平铺无分组头；颜色改色点胶囊与 #标签 并列分行。

### O5' · 拼贴语言统一（R3 · R5）

- OutfitThumb 无成品图时改用 BodyCollage 迷你人形拼贴（与卡组同语言，含空槽）；
  记录网格恢复日期显示（R6-P1 反馈信息薄）。
- 人形轮廓底 alpha 0.35→0.5（对比度 P2）。

### O6' · 导出面板三修（R8，含 bug）

- **预览自适应**：固定 300dp → 屏高 42%（长图全貌可期）。
- **动作钉住**：sheet 改「滚动区 + 底部固定动作栏」结构——复制长图/分享/只复制文本永远可见，
  展开四维不再挤走（R2 实测问题）。
- **记忆竞态 bug 修复**：PrefsStore/AppViewModel 增 `exportSelectionsReady` 首发射标志，
  恢复逻辑等 ready 或非空才 init（杀进程重开选择不再丢）。
- 折叠条「已选 N」计数改 accent 徽标（P2）；人物描述 label 缩短为一行（P2）。

### O7' · 详情与表单（R4 · R7）

- 「＋录入成品图」角标从拼贴右下移到右上（远离「未配鞋」空槽语义区）。
- 「复制素材」更名「复制长图」（与 W1 同一套词）。
- **照片必填大占位（N5）**：新建时照片区显示 aspect 0.8 虚线预览占位（相机图标 +「拍照 / 选照片 · 第一步」），
  点击即选；已有照片时保留预览 + 更换行。
- 保存栏重构（与 eats it-005 同构）：未就绪中性灰原因 + 全宽两态按钮 + imePadding 最外层；
  原因文案只在点击时以 toast 强调。

### 轻问题顺手修（R1 · R2）

- R1 品类徽标并入卡底名称条（「品类 · 名称」），释放左上角，‹n/n› 独占右上。
- R2 「✓ 使用中」紧跟角色名（不再悬在行末）。
- 跳过（记录理由）：鞋槽 Fit 过小（it-010 Fit 决策权衡，衬纸 padding 4→2dp 缓解）、
  图例交互/marker 形状（新功能）、emoji 基线（系统渲染）。

## 验证记录

- **构建**：`assembleDebug` ✅（2026-09-20）
- **模拟器实测**（截图 /tmp/verify-r3/；uiautomator 断言 + 像素级检查）：
  - **R5-P0 ··· 菜单**：点 ··· 弹「编辑/删除」菜单（bounds 实测 471,616/471,742）✅；长按保留
  - **R5-P1 筛选固定**：筛选 chip 首屏可见 @(917,302)，无需横滑 ✅（修复 R2 实锤的溢出）
  - **R5**：品类图标圆 Tab 一屏 6 枚+筛选；去小节标题；颜色改胶囊 ✅（截图）
  - **R3**：下方网格统一为迷你人形拼贴（含未配帽/未配鞋空槽）✅（截图）——双拼贴语言问题消除
  - **R4**：「＋录入成品图」角标移拼贴右上 ✅（截图）；「📋 复制长图」命名统一（截图实锤）
  - **R7**：新建表单大虚线占位「拍照 / 选照片 · 第一步」✅；未就绪原因「选照片、填名称后可保存」✅；编辑态「更新」全宽吸底 ✅
  - **R8-P1 记忆竞态 bug**：选「通勤简约」→ 杀进程重开 → prompt 含「氛围：通勤简约」✅（修复生效，exportSelectionsReady 首发射标志）
  - **R8-P1 动作钉住**：展开四维前后「只复制文本」均 y=2248 固定 ✅
  - **R8 附加修复（历史 bug）**：预览区自 it-002 起一直空白（内层 verticalScroll 无限高度约束使 Coil 请求尺寸失效，R1/R2 截图可证）——改 `heightIn(42%)+ContentScale.Fit`，像素检查 stddev 2.0→21.4，长图全貌一屏可见 ✅
  - R1 品类徽标并入名称条、R2 ✓使用中紧跟名字、轮廓对比度 0.5、衬纸 padding 2dp 均落地
