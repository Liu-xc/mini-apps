# CHANGELOG · 吃啥 (eats)

## 未发布

- **it-008**（2026-09-20）：吃喝玩乐——Place 一级分类（吃/喝/玩）+ kind 语义泛化零迁移；「今天干啥」动态标题/分类筛选/地图三色；想吃想玩愿望清单（种草/只抽愿望/落账自动拔草可撤销）；愿望排期（安排到某天/最近的安排/当日提示）；3 项 UI 走查优化（切分类重置类型/去处文案/无障碍）。（ADR-014）

### it-007 — 统计回顾：年度食光 + 好久没去提醒
- 新增 W7 统计回顾页（今年/去年/累计三档：总顿数/总花费/去过店数、最爱 TOP3、类型占比、
  月度节奏、连续记录；入口=列表页标题行统计图标，US-09）；
  「年度食光」长图（封面/三大数字/最爱/占比/月度/照片墙 ≤9/结尾，存 Pictures/Eats + 分享，US-10，
  长图自实现不抽 libs，ADR-012）；
  WorkManager「好久没去」本地提醒（去过 ≥3 次·评分 ≥4·超 N 天没去，每日至多 1 条，默认关，
  BigPicture 大图，点击深链进详情，ADR-013，US-11）；RecapCalculator/MemoryCandidateSelector 纯函数单测。

### it-006（进行中）— 数据层换用 libs/store + 演示模式
- it-006 数据层换用 libs/store（SnapshotStore/SsotRepository/FileMediaStore，ADR-010，磁盘格式不变）+
  应用内演示模式：内存 Mock 数据源（DemoMode 开关/重启生效，入口仅 DEBUG 可见，进/出同一入口确认；修订：去横幅）；
  顺带修复 01-user-stories.md 被 it-003 提交污染为 2.4MB 重复块的问题（恢复健康版+重放修订）。

- 卡组层级修正（it-010 同批）：容器裁剪防越界、堆叠偏移 30→14dp、卡片投影 4→2dp
- it-003 转盘 → 侧滑卡组（libs/carddeck SDK，薄封装 compose-swipeable-cards）：信息卡含照片/类型/
  评分/上次/标签/下单链接，侧滑浏览 + 老虎机式纯随机抽取（无权重，ADR-011），移除 WheelCanvas/SpinWheel。
- R6 主题重塑：暖橙（番茄红/琥珀）整体切换为清新草绿系——主色嫩芽绿 #3FA265、类型语义色换
  青绿/湖蓝/草绿、评分星固定金色惯例、转盘指针改墨色、彩屑换清新四色、启动图标淡绿底、暗色墨绿纸。
- it-002 信息流与交互全面升级（5 轮评审）：列表↔详情共享元素过渡、瀑布入场、滑动删除、
  长按快速记一笔、落账 ✓+彩屑编排、转盘拍动指针与中心扫掠、时间线日期母题、
  未评分弱化、暗色 surface 层级与 FAB 对比度修复、「在地图上看」退栈修复。
- it-001 MVP：堂食/外卖/自做三类食堂录入（地图长按选点、链接粘贴自动识别、评分/标签/照片/笔记）、
  记一笔 Visit（时间默认现在，可附评分/花费/感想/照片）、今天吃啥加权转盘（类型/忌口/最近吃过过滤，
  抽中直达下单链接）、地图总览（类型着色 marker、摘要卡、未上地图入口）、列表（排序/筛选/搜索）、
  详情时间线与派生统计。单 JSON 原子存储 + WebP 图片压缩，30 个 JVM 单测。

## it-004 · UX 评审落地（交互补课）2026-09-20

- 报告：`reports/2026-09-20-ux-review/`（15 页 P0/P1 全量）→ 本迭代吃掉 E1–E7
- O1 卡组可发现性（‹n/m› 卡序+候选数、筛选收纳弹层）与抽取落定闭环（深色结果块+按钮常驻屏内）
- O2 地图图例胶囊+⌖回位+类型色回归 W2 基线（红/琥珀/绿）
- O3 表单吸底保存两态+imePadding；O4 详情记一笔吸底+统计一套口径+链接 chip 去冗余图标
- O5 列表筛选收纳单行工具条（未定位归位为状态项入弹层）
- C3 键盘避让实测修复（弹键盘时底部导航仍在屏内）

## it-005 · R2 评审落地（打磨收口）2026-09-20

- 报告：`reports/2026-09-20-ux-review-r2/` → O1' 落定重心下沉（收拢加深/彩屑双发）、O2' 筛选 AssistChip+胶囊出卡面+链接收行+露边 18dp、O3' 保存栏 Column 重构（中性原因+全宽两态+点击提示）+位置入口样式、E4b/E5/E6 细节
- 启动图标换中式「鸳鸯火锅」（thiings 素材，署名不变）：拉面碗 → 中式大餐感（先换烤火鸡，Leo 要求中式后改火锅）
