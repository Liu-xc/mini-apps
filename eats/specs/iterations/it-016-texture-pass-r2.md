# it-016 · 质感 pass R2：触控目标 + 彩屑预算 + 入场例外清理

- **状态**：实施中（2026-09-21 Leo 指示「持续优化」，DESIGN.md 对表第二遍）
- **对表基准**：[DESIGN.md](../../../DESIGN.md) §2.5 / §3

## 审计发现与修复

| # | 发现（条款） | 修复 |
|---|---|---|
| 1 | 评分星输入命中区 = 星尺寸+10dp（默认 28dp，§2.5 ≥44） | 输入模式命中区 `coerceAtLeast(44dp)`，视觉星尺寸不变；展示模式（无 onChange）不受影响 |
| 2 | 详情页 Visit 删除钮命中区 28dp（§2.5） | IconButton 28→44dp，视觉图标 16dp 不变 |
| 3 | PhotoStrip 照片移除角标命中区 ~22dp（§2.5） | 角标命中区 36dp（媒体角标例外档，见 DESIGN.md §2.5 增补），视觉不变 |
| 4 | 「随机抽一张/再抽」一次落定触发**两份**彩屑（§3 预算：庆祝 ≤1 次/动作） | 统一为落定单份彩屑：随机抽一张去掉 400ms 后第二发；再抽去掉起抽时第一发 |
| 5 | 触感已覆盖（it-015），本轮无新增 | — |

## 验收标准

- Given 评分输入（记一笔），Then 每颗星命中区 ≥44dp、视觉 26dp 不变
- Given 抽中一次落定，Then 恰好一份彩屑 + 一次 confirm 震感
- Given 详情删 Visit / 照片移除角标，Then 命中区达标、误触率不升
- 构建与单测全绿；模拟器冒烟无崩溃

## 验证记录（2026-09-21 回填）

- 构建 + 单测：`./gradlew :app:assembleDebug :app:testDebugUnitTest` 全绿——**53 tests, 0 failures**。
- 模拟器冒烟（emulator-5554）：安装启动正常、进程存活、logcat 零 FATAL（RatingStars 命中区与彩屑单一化为交互行为，截图无法断言，代码层验证 + 启动无回归）。
- 彩屑单一化：随机抽一张路径删去 400ms 后第二发、再抽路径删去起抽第一发——一次落定恰一份彩屑 + 一次 confirm。
