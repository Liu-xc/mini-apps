# it-013 · 干啥页抽中态重排 + 筛选可见化（UX 评审报告落地，eats 侧）

- **状态**：实施中（2026-09-21，ux-report-impl worktree；编号让位说明：UX 报告原拆分建议写 it-012，该号已被「数据包」提案占用，顺延为本号）
- **来源**：[UX 评审报告 2026-09-21](../../reports/2026-09-21-ux-review/) E1·P1 + P2×2 + C2/C3

## 范围（对照报告落地拆分）

1. **O1（P1）抽中落定重排**：结果块移入卡组区作居中 overlay、撑满原区（消除抽中后 ~40% 屏空档）；
2. **O2（P1）单一出口**：抽中态底部按钮组隐藏，替换为「换一张（返回卡组）」单一出口（与块内「再抽」不打架）；
3. **O3（P2）卡片留白**：PlaceCard hero 由固定 212dp 改 `weight(1f)`（卡面增高时图片随之撑满，消除卡底大空白）；
4. **P2 筛选可见化**：分类/类型/只抽愿望任一非默认时，chips 行尾出现「重置筛选」chip（一键回全量）；
5. **P2 FAB 遮挡（复核项）**：列表 LazyColumn 已有 bottom=96dp contentPadding，报告截图疑为滚动中常态——模拟器滚到底实测后决定是否加大（复核结论回填验证记录）。

## 不做

- 地图噪条（C3）：报告已标「疑截图管线伪影，待真机复核」，不基于模拟器证据修改渲染。

## 验证记录

- 2026-09-21：eats 49/49 单测绿；模拟器实测——抽中态新布局与线框一致（结果块撑满卡组区、内容居中、块内双按钮、底部单一出口，e1 截图）；「重置筛选」chip 实测出现于行首（首版放行尾被横向滚动卷出屏，实测发现后移行首）、点击回全量后消失；卡片 hero 弹性后卡底空白消除。FAB 复核：滚到列表底末行完整可见（bottom=96dp 足够，报告截图为滚动中常态）→ 不改代码，该项降级为非缺陷。全程 0 crash。
