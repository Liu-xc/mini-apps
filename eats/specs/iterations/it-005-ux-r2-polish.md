# it-005 · UX 评审 R2 落地（打磨收口）

- **状态**：已完成
- **提案日期**：2026-09-20
- **来源**：`reports/2026-09-20-ux-review-r2/`（用户确认「完整实施优化」）
- **范围**：R2 报告 eats 侧全部 P1 + 可落地 P2（O1'–O3' + 轻问题清单 E3–E6 项）

## 变更清单

### O1' · 抽取落定重心下沉（E2）

- 卡组收拢幅度加大（alpha 0.25→0.15、scale 0.9→0.82），结果块成为唯一视觉主体。
- 落定态隐藏底部原动作行（已实现，保持）；结果块与底部导航间距收 12dp。
- 彩屑两连发（trigger +2 次，间隔 ~400ms），高潮氛围停留延长。

### O2' · 首页筛选差异化 + 卡片减负（E1）

- 「筛选」从 FilterChip 改 AssistChip（描边 + 漏斗前缀图标），与类型 chips 视觉分离；角标计数保留。
- ‹ n/m › 卡序胶囊移出卡面：从 deck 顶部 overlay 改为筛选行下方独立居中行，不再压后卡露边。
- 多链接收一行：卡片只展示第一条链接 chip（附「+n」计数），其余进详情页（E5 已有完整列表）。
- 堆叠露边 14→18dp（P2）。

### O3' · 列表/表单细节（E4 · E7）

- 搜索 placeholder「搜名称 / 菜系」→「搜名称/菜系」（防截断）。
- 保存栏重构（与 wardrobe R7 同构）：Column 结构 = 未就绪中性灰原因（不再用禁用按钮内嵌）+ 全宽按钮
  （未就绪描边/就绪实心）；imePadding 提到最外层消除键盘缝隙；点击未就绪按钮 toast 具体原因。
- 「位置」选点入口加描边 + 右缘 › chevron，与普通输入框区分。

### 轻问题顺手修（E3–E6）

- E4b 筛选弹层：类型/标签分组间距 +12dp。
- E5 统计行 labelMedium→bodyMedium，间距放宽。
- E6 落账按钮与键盘间距 24→28dp。
- 跳过（记录理由）：图例可点筛选（新功能）、marker 形状区分（跨能力）、地图首开瓦片延迟（网络特性）、
  ⌖ 前缀跨机型风险（本机正常）、hero 渐变过渡（视觉权衡）。

## 验证记录

- **构建**：`assembleDebug` ✅（2026-09-20）
- **模拟器实测**（截图在 /tmp/verify-r3/，前后对照报告内嵌）：
  - E1：「筛选」AssistChip（漏斗前缀+描边）与类型 chips 分离 ✅（截图）；‹ 1/3 › 胶囊独立于卡组上方 ✅；露边 18dp 可见 ✅
  - E2：卡组收拢 alpha 0.15/scale 0.82，「✓ 就吃这个/再抽」仍在可视区（回归 PASS）；彩屑双发
  - E7：保存栏 Column 重构——未就绪中性灰原因（y=2081）+ 点击 toast 提示；就绪全宽实心随键盘上移 ✅
  - E4/E4b/E5/E6：搜索 placeholder 缩短、筛选弹层分组间距、统计行 bodyMedium、落账间距 28dp 均落地
- 跳过项按提案记录（图例交互/marker 形状/瓦片延迟/⌖ 跨机型/hero 渐变）
