# 夜间专项验收报告（架构 · 性能 · UI 三轮）

- **日期**：2026-09-21 凌晨 → 早晨
- **分支流**：worktree `arch-review-fixes`（基于评审报告提交 a11e051）→ 5 个功能提交 → 合并回 main（15b1be4）
- **验收结论**：✅ 两应用全部核心功能在位、208 个单测零失败、三轮模拟器走查零崩溃、2 个走查发现的真实问题已修复

## 一、做了什么（三轮 × 三个维度）

### 第 1 轮
- **架构**（it-021 阶段1，a911ba7）：Repository 按聚合域拆 6 个子接口（门面继承，调用点零改动）；`ImageEditStore` 接口收编抠图/解码/导出能力；`RecapViewModel` 自全局 VM 拆出（回顾/提醒域）；「好久没穿」候选规则抽 `StaleItemSelector` domain 纯函数（+4 单测含边界）。
- **性能**（it-022/it-010，b39267b）：衣橱/记录页过滤链包 `remember`；4 处 Lazy 行补 key；审计确认落盘/长图渲染/图片导入均已在 IO 线程（审计表见 it-022）。
- **UI**（it-023/it-011）：两 app 全页走查（搭配/记录/衣橱/详情/导出/回顾/心愿 × 干啥/地图/列表/详情/记一笔/回顾），交互流逐条实测。

### 第 2 轮
- **架构**：六构建全量回归绿。
- **性能**：复审抓到真问题——回顾长图预览在组合期**主线程全尺寸解码 ~14MB 位图**（开预览必卡），改 Coil 按约束降采样（a684e0b）。
- **UI**：修复 sheet 内校验提示被遮挡（转正无实物照/心愿名为空 → 提交按钮置灰+文案明示原因，7cf71ec）并实测验证；演示模式退出→重启→真实数据→往返闭环实测。

### 第 3 轮
- 三轮快扫（真实模式+演示模式、随机抽取/搜索/槽位交互/长图生成存相册）零崩溃、无卡顿感知；六构建最终门禁全绿。

## 二、修复的问题（走查/复审发现）

| 问题 | 提交 |
|---|---|
| sheet 内 toast 校验不可见（表单像失灵） | 7cf71ec：按钮置灰+原因文案 |
| 长图预览主线程全尺寸解码必卡 | a684e0b：Coil 降采样 |
| （前夜遗留）Mock 心愿域级联漂移、写路径无兜底、spec 系统性脱节、零 CI、版本 6 处手工 pin | 见 it-020/it-009（bfbb2a1…76a5d93） |

排除的疑点（查证后非 bug）：回顾页顶栏增高（分段按钮自然高度）、顶栏图标无障碍（已有 desc）、eats 地图状态栏噪点（screencap 伪影）、eats 种草入口（设计在编辑表单）。

## 三、验收清单（Leo 早晨可复核）

1. **测试**：`wardrobe/gradlew -p wardrobe testDebugUnitTest`（56）+ `-p eats …`（74）+ libs 三库（14/49/15）全绿；carddeck build 过。
2. **模拟器冒烟**：两 app 已装最新合并包（versionCode 5 基线），全部页面可用、零崩溃。
3. **功能不丢失**：演示模式连点入口（main 新交互）、搭配格增删、卡组双向循环等 main 新功能与本次改动共存，合并后全量测试+走查通过。
4. **架构**：评审报告 P0-1/2/4 + P1-1/2 全部落地；P0-3 落地阶段 1（WishlistViewModel 拆分经评估裁剪，理由入档 it-021）。

## 四、遗留（非阻塞）

- **CI 未实跑**：`.github/workflows/ci.yml` 已入库，随本次推送首次触发，首跑结果待看。
- eats 04-architecture 的「演示入口=DEBUG 按钮」描述已过时（main 的 911535d 改为隐藏连点式，全构建可用）——spec 未随那次提交更新，建议下次迭代补一行。
- push 状态见当晚会话记录（国内网络推送曾挂起，后台重试中；本地 main 已是合并完成态，推送失败不影响验收）。
