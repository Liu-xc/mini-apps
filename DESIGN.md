# DESIGN.md — mini-apps 仓库级 UI/交互质量基准

> 状态：**v1.0 生效（2026-09-21 Leo 确认，随质感迭代 wardrobe it-027 / eats it-015 落地）**。任何 UI/交互相关迭代的提案与走查均以本文为对表依据；AGENTS.md「上下文恢复」已引用本文。

## 定位

本文件定义**跨应用家族基线**：所有 App 共享的硬 token、动效规则、质感红线与评审 query。各应用的 `specs/05-design-system.md` 在此基线之上定义各自个性；**基线是红线（不可破），个性是选择（必须明确声明）**。

两处冲突时：视觉值以应用 spec 为准（个性优先），规则与红线以本文为准（基线不可协商）。

## 1. 家族基调与个性轴

统一骨相（所有 App 一致）：淡绿纸感 `paper` 底、衬线 Display 大标题 + Sans 正文、内容（照片/数据）是绝对主角、装饰克制、层次靠留白与 hairline 而非阴影。

| App | 个性定位 | 个性表达 |
|---|---|---|
| wardrobe | 时装编辑（print） | 衬线最重、黑白绿三色、4:5 编辑排版、唯一强调色 |
| eats | 餐牌手帐 | 轻快一档、允许类型语义色与金色评分入画、时间线母题 |
| clips | 速记工具（待定稿） | 更快更素：动效短促、装饰最少、快进快出 |

新 App 接入：在此表加一行，并声明它对基线的偏移点。

## 2. 硬 Token（基线，值可按 App 微调，结构不可缺）

### 2.1 色彩结构

六个语义 token 必须存在：`paper`（背景）/ `surface`（卡片弹层）/ `ink`（主文字）/ `inkFaint`（次级）/ `accent`（强调）/ `hairline`（分隔）。每个 token 必须有深色对值；深色是「墨绿纸」同构重绘，不是亮度反转。

- 惯例语义色**不随 accent 走**：评分金（eats `#E0A93E`）、类型三色（eats 红/琥珀/绿）、系统 error（破坏性操作）。
- accent 只做强调：强调色屏幕占比 < 10%（按钮/选中态/FAB 级别），禁止大面积铺底。

### 2.2 对比度（WCAG AA 为底线）

| 组合 | 下限 |
|---|---|
| ink / paper | ≥ 7:1 |
| inkFaint / paper | ≥ 3:1 |
| 白字 / accent | ≥ 4.5:1 |

### 2.3 排版

| 层级 | 规格 |
|---|---|
| Display | 衬线（Noto Serif CJK）semibold 28–34sp，仅页面大标题/实体名 |
| Title | 衬线 20sp，卡片标题 |
| Body | Sans 15sp |
| Label | Sans 11sp，letterSpacing 0.2em |

红线：中文正文一律 Sans（衬线小字号易糊）；Label 用于「印刷小标签」母题，不用于正文说明。

### 2.4 形状与间距

卡片圆角 20dp · 弹层顶角 28dp · chip 8dp · 屏幕边距 20dp。阴影极轻或无，禁止阴影+圆角+渐变三件套堆叠出拟物感。

### 2.5 交互基础

- 触控目标 ≥ 44dp；功能图标一律 Material Icons，**emoji 不得作为功能图标**。例外（it-028 增补）：媒体卡（照片/图片卡）上的悬浮角标（删除/菜单钮）命中区 ≥ 36dp 即可，视觉尺寸不变。
- 破坏性操作必须二次确认 **或** 提供撤销（snackbar），二选一不可都无。
- 禁止弹窗套弹窗、sheet 嵌 sheet（前车之鉴：it-006 对话框误嵌）。

## 3. 动效规则（EditorialMotion）

弹簧基调三件套（两 App 代码中的现行值，即基准值）：

| 弹簧 | 参数 | 用途 | 禁止用途 |
|---|---|---|---|
| `smooth` | noBouncy + StiffnessMediumLow | 常规位移、内容切换 | — |
| `pop` | damping 0.72 / stiffness 500 | 落定、确认、选中、弹入 | 大面积列表逐项（过冲×N 头晕） |
| `bouncy` | MediumBouncy + stiffness 700 | 随机落定、庆祝、彩蛋 | 常规导航（弹跳是特权不是默认） |

节奏与预算：

- 交互反馈 < 100ms 起步（点击必须立刻有视觉/触感回应，动画慢是另一回事）。
- 入场 stagger 20–24ms/项；crossfade 220ms；彩屑等庆祝动效 ≤ 900ms 且**只挂在值得庆祝的动作上**（落账/复制/抽中）——删除、报错永不庆祝。
- 一屏内自主运动的元素 ≤ 1；一层动效只负责一件事，弹簧不叠加。
- 超过 200ms 的入场编排只允许首屏/首次进入，返回与二次进入走快路径。
- 跟随系统「移除动画/减弱动态」无障碍设置整体降级。

## 4. 触感与声音（2026-09 新基线；已随 wardrobe it-027 / eats it-015 落地）

- 关键确认动作（保存成功、复制、打卡、抽中落定）给一次 `HapticFeedbackType.Confirm`（或 LongPress 兜底）轻震；错误用 DoubleTick 类警示震。
- 滚动、普通导航、输入不加触感。
- 不加音效（个人工具，公共场合使用）。

## 5. 反例清单（红线，走查直接对表）

1. 无意义入场动画拖慢二次进入（本条即 §3 预算）。
2. emoji 当功能图标；或 图标+文字+色块 三重编码同一语义。
3. 彩屑出现在非庆祝动作。
4. accent 大面积铺底、或强调色超 3 种同屏。
5. 深色模式纯 `#000` 底 / 纯 `#FFF` 字（未按墨绿纸重绘）。
6. 对比度不达标的灰字大片（尤其 11sp Label 上的低对比）。
7. 删除无确认且不可撤销。
8. 空状态只有一行字——必须有插画/图形 + 行动按钮（两 App 已有此惯例，成为基线）。
9. 数字变化无过渡（统计数字、计数类直接跳变）。

## 6. 评审 query 模板（给 AI 会话与自走查）

- **走查**：「按 DESIGN.md §2–§5 逐条对表以下截图（附图），输出 P0（违红线）/ P1（违基线）/ P2（可打磨）清单，每条注明违反的条款号。」
- **新界面**：「先声明此界面在个性轴上的定位与使用的 token，再给布局与动效编排；动效逐个过 §3 用途与预算。」
- **动效验收**：「对照 §3 表格判断每个动效的弹簧选择、时长、触发对象是否越权（bouncy 用于常规导航？彩屑挂在删除上？）。」

## 7. 治理

- 修改基线 = 修改本文 + 受影响应用 `05-design-system.md` 同步 + 必要时 ADR。
- 每个 UI 类迭代收尾时，走查结论（query 模板产出）回填该迭代 it-XXX 的验证记录。
