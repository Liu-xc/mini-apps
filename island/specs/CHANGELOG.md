# 更新日志

格式参照 Keep a Changelog；版本号遵循语义化。链接各迭代提案。

## 未发布

### 展示形态：并排双源环形面板（it-002 增补 3）
- 三环同屏方案推翻重来：**并排双源面板**——GLM 面板（双环：外 5 小时/内 每周）+ MiMo 面板（单环），
  各带 白 4.5% 圆角面板底 + 描边、环心内容源名、面板内图例（色点/标签/重置/百分比），定高对称。
- 未配置凭证的面板显示引导（未配置 API Key/Cookie → 去设置）。

### 展示形态：三环同屏 + 健康度配色（it-002 增补 2）
- **去掉内容源切换 chips**：所有已配置内容源**同屏展示**——Apple 健康式三环
  （外环=5 小时、中环=每周、内环=MiMo）+ 右侧三行图例（色点/标签/重置时间/剩余%）。
- **颜色=健康度**：剩余 ≥50% 绿、20–50% 橙、<20% 红（环与图例同色）——
  修正「MiMo 99% 配橙色像不健康」的违和；恒定身份色方案废弃。
- 行高随源数自适应（图例块与环簇取高者）。

### 展示形态：Apple 健康式同心环（it-002 增补）
- 明细从线性进度条改为 **Apple 健康式同心环**：外环=5 小时（蓝）、内环=每周（绿）、MiMo=单橙环；
  填充比例=剩余量，round cap + 沿弧身份色渐变（55%→本色）、白 12% 底轨、环心显示内容源名；
  环簇 80×80 定高居左，右侧图例行（色点+标签+重置时间·百分比），行高公式改为环簇定高。

### it-002 — 多内容源架构 + 小米 MiMo TOKEN Plan 接入
- **多内容源**：`ProviderKind`（glm/mimo）+ 按源分发拉取、按源独立快照缓存与钥匙串账户
  （glm-key/mimo-cookie，旧 api-key 自动迁移）；卡片顶部内容源切换 chips + 菜单栏「内容源」单选；
  切换即时呈现该源缓存并后台刷新（ADR-008 落地第一步）。
- **MiMo 接入**：`GET platform.xiaomimimo.com/api/v1/tokenPlan/usage`（仅浏览器 Cookie 认证，
  tp- key 不被接受）；percent 为小数比例，剩余=(1−percent)×100，limit=0 条目跳过，无重置时间；
  Cookie 过期页脚明示「请在设置更新」。设置页新增 MiMo Cookie 区。
- 行高公式含 chips 行；identityColor 增加 MiMo 橙；swift-testing 17/17。

### it-001 — 灵岛（island）：刘海功能入口 + GLM Coding Plan TOKEN 监控 MVP
- **更名与定位（Leo 反馈）**：「GLM 灵动岛」→**「灵岛 / island」**：定位为通用刘海功能入口容器，
  GLM TOKEN 只是首个内容源，未来扩展其它厂商与能力（ADR-008）；目录/BundleID/钥匙串 service 一并迁移。
- 数据层：官方 monitor 接口客户端（国内 bigmodel.cn / 国际 z.ai 双端点 + auto 探测记忆偏好，
  `Authorization` 直放 Key 无 Bearer 前缀）；`limits[]` 宽松解析（候选键 + 顺序兜底），
  原始响应带回设置页诊断，**字段待 M0 spike 真实响应校准**；
- 快照缓存落盘（断网/重启显示陈旧数据）；Key 只进 Keychain；
- 刘海胶囊双态：紧凑 118×26（三迷你条）↔ 展开 352×228（三行明细 + 刷新/设置），
  hover 展开/移开收起/点击固定/点击外收起，spring 动画 + 轻触感；无刘海屏回退顶部居中；
- 状态机：剩余 ≤20% 警戒橙、=0% 红「已用完 · X 重置」；紧凑迷你条同步变色；
- 设置窗口：Key 管理 / 端点 / 间隔 1-30 分钟 / 演示模式 / 开机自启（SMAppService）/ 诊断原始响应；
- 菜单栏图标（三彩条）+ 用量摘要菜单；演示模式（假数据走查 UI，不请求接口）；
- 构建：SPM + CLT（无 Xcode），swift-testing 14 测全绿；`tools/make-app.sh` 打包 ad-hoc .app；
  `tools/spike-usage.sh` 一键抓真实响应；
- 已知限制：系统全屏截图不合成该层窗口（`screencapture -l` 可验，不影响真实显示）；
- 落位修复 v2（Leo 二轮反馈「还差很多 + hover 一直闪」）：避让方案胶囊落在刘海左缘与刘海分离，
  改为**刘海正中锚点**——刘海挖槽天然无冲突，紧凑态融进刘海黑区、展开态贴顶向下生长（ADR-005 定稿）；
  hover 闪烁根因 = 窗口变形动画中窗口服务器补发成对 enter/exit 引发振荡，
  修复 = 假离开守卫（exit 时光标仍在面板内则忽略）+ enter 60ms / exit 180ms 可取消防抖；
  CGEvent 模拟悬停实测：一次展开→稳定→真离开一次收起，零振荡。
- 触感移除（Leo 反馈「不要震动」）：hover 展开不再触发 NSHapticFeedbackManager，动画保留。
- 信息结构调整（Leo 反馈）：**ZCode MCP 档不再展示**（解析/数据保留，displayRows 过滤）；
  紧凑态升级为**两行全信息**（180×36）：每档「标签 + 迷你条 + 百分比 + 重置时间」，
  不展开即可读全量；演示数据每周改为 18% 以展示警戒橙。
- 落位修复 v4（Leo 反馈「默认不该展示、hover 刘海再动画展开、顶部与刘海间有缺角」）：
  **默认完全隐藏**——窗口即刘海挖槽矩形（180×safeTop 全透明），隐形但可收 hover（ADR-006）；
  hover 刘海动画展开 352×178 卡片，**顶边贴屏幕顶沿、顶部两角直角**，与刘海/顶边之间无缺角；
  移开动画收起。CGEvent 实测：隐藏(645,0,180×32) → 一次展开(559,0,352×178) → 移开一次收起，零振荡。
- 动画与 UI 减法（Leo 反馈「动画应从刘海向下延伸而非斜向生长、文字避开刘海、整体太花」）：
  生长动画改为 **SwiftUI 圆角遮罩 reveal**——窗口瞬移就位，遮罩矩形在 刘海尺寸↔卡片全尺寸 间
  spring（**宽高一起**：展开从刘海长出、收起对称吸回；先渲染起始帧再延迟 50ms 触发，ADR-007）；
  卡片内容顶边 = safeTop+6 避开刘海；
  简化：删演示徽章/顶部高光线/冗余提示块，标签改白色、颜色只留进度条，行收敛为
  「标签 + 百分比 · 重置时间 / 进度条」，演示状态并入页脚文案。
- 收起动画修正（Leo 反馈「宽度也应一起缩，现在是先变矮再被藏进刘海」）：遮罩方案天然对称——
  收起时宽高同时缩回刘海尺寸，中间帧为对称缩小的小黑卡；窗口在动画结束后才瞬移归隐。
- 去掉「剩余额度」标题（Leo 反馈）：卡片只留明细行 + 页脚，高度公式同步收紧。
- **M0 spike 完成，真实数据上线**（Leo 提供 Key）：双端点实测 200 且返回一致；解析器按真实结构
  收紧——`percentage`=已用%（剩余反推）、`remaining`/`currentValue`/`usage`=绝对 token 数勿当百分比、
  `unit` 3/6 分档、`nextResetTime` 毫秒时间戳；真实响应入 fixture（15 测试）。
- 行内顺序调整（Leo 反馈）：**重置时间在前（灰小字）、百分比在后（粗体）**。
- 修复 id 撞车：type 恒为 CREDIT_LIMIT 使两行 id 相同 → ForEach 重复渲染同一行；
  id 改为按最终 kind/唯一化生成。Key 入钥匙串（App 自写 ACL，GLM_ISLAND_SEED_KEY 注入），
  真实数据与控制台一致。
